#include <string.h>

/**
 * _strlen - returns the lenght of string value of s
 *
 * @s: string variable
 *
 * Return: returns the lenght of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
