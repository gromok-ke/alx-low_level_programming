/**
 * _abs - prints the absolute value of an int
 *
 * @a: int
 *
 * Return: - id value is less than zero and positive otherwise
 */

int _abs(int a)
{
	if (a < 0)

		return (-a);

	else
		return (a);
}
